Place binary assets referenced from data/profile.json here, mirroring the
path you use in the JSON. Vite copies everything under public/ verbatim to
the site root on build, so a JSON path of "/assets/images/profile.jpg"
should map to a real file at public/assets/images/profile.jpg.

Also put your resume PDF at public/assets/resume.pdf (or wherever
personal.resumeUrl points).
